scenarios_path: data/sample/scenarios.jsonl
checkpointer: memory
# CONTRACT: cli.run_scenarios overwrites this file on every run via report.write_report.
# It must NOT point at reports/lab_report.md — that is M5's hand-written submission.
report_path: reports/lab_report_generated.md
